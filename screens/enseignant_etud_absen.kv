<ProfEtudAbsenScreen>
    name: "ens_absences"
    MDBoxLayout:
        orientation: "vertical"

        MDTopAppBar:
            title: "Absences Signalées"
            left_action_items: [["arrow-left", lambda x: app.back_button_ens()]]
            elevation: 2
        ScrollView:
            do_scroll_x: False
            effect_cls: "ScrollEffect"  # Pour le scrolling fluide
            bar_width: 0  # Cache la barre de scroll
            MDBoxLayout:
                id: absence_box
                orientation: "vertical"
                padding: "10dp"
                spacing: "10dp"
                size_hint_y: None
                height: self.minimum_height